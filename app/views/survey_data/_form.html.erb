<%= form_for(@survey_datum) do |f| %>
  <% if @survey_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_datum.errors.count, "error") %> prohibited this survey_datum from being saved:</h2>

      <ul>
      <% @survey_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session %><br>
    <%= f.text_field :session %>
  </div>
  <div class="field">
    <%= f.label :question %><br>
    <%= f.number_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
